# Automated Backup System

# How to run (Dec 2025)

1. git clone this repo
2. One time only: docker network create project-net
3. cd docker
4. docker compose up -d --build
5. Put files into ../source folder → they appear in ../backup

